services:
  javbeaconsubs:
    build:
      args:
        WHISPER_IMAGE: ghcr.io/ggml-org/whisper.cpp:main-cuda
    environment:
      JAVBEACONSUBS_USE_GPU: "true"
      JAVBEACONSUBS_GPU_PREFLIGHT: "true"
      JAVBEACONSUBS_GPU_AUTO_RESET: "${JAVBEACONSUBS_GPU_AUTO_RESET:-true}"
    gpus: all
